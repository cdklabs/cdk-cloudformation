# ~~ Generated by projen. To modify, edit .projenrc.js and run "npx projen".

name: release-aqua-enterprise-kubeenforcer
on:
  workflow_dispatch: {}
jobs:
  build:
    runs-on: ubuntu-latest
    permissions:
      contents: read
    steps:
      - name: Checkout
        uses: actions/checkout@v2
      - name: Set git identity
        run: |-
          git config user.name "github-actions"
          git config user.email "github-actions@github.com"
      - name: build:aqua-enterprise-kubeenforcer
        run: npx projen build:aqua-enterprise-kubeenforcer
      - name: Upload artifact
        uses: actions/upload-artifact@v2.1.1
        if: always()
        with:
          name: packages/@cdk-cloudformation-types/aqua-enterprise-kubeenforcer/dist
          path: packages/@cdk-cloudformation-types/aqua-enterprise-kubeenforcer/dist
    container:
      image: jsii/superchain:1-buster-slim
