# ~~ Generated by projen. To modify, edit .projenrc.js and run "npx projen".

name: release-awsqs-iridium-cloudconnectqs-module
on:
  workflow_dispatch: {}
jobs:
  build:
    runs-on: ubuntu-latest
    permissions:
      contents: read
    steps:
      - name: Checkout
        uses: actions/checkout@v2
      - name: Set git identity
        run: |-
          git config user.name "github-actions"
          git config user.email "github-actions@github.com"
      - name: build:awsqs-iridium-cloudconnectqs-module
        run: npx projen build:awsqs-iridium-cloudconnectqs-module
      - name: Upload artifact
        uses: actions/upload-artifact@v2.1.1
        if: always()
        with:
          name: packages/@cdk-cloudformation-types/awsqs-iridium-cloudconnectqs-module/dist
          path: packages/@cdk-cloudformation-types/awsqs-iridium-cloudconnectqs-module/dist
    container:
      image: jsii/superchain:1-buster-slim
