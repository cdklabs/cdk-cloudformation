# ~~ Generated by projen. To modify, edit .projenrc.js and run "npx projen".

name: release-awsqs-kubernetes-resource
on:
  push:
    branches:
      - main
  workflow_dispatch: {}
jobs:
  build:
    runs-on: ubuntu-latest
    permissions:
      contents: read
    steps:
      - name: Checkout
        uses: actions/checkout@v2
      - name: Set git identity
        run: |-
          git config user.name "github-actions"
          git config user.email "github-actions@github.com"
      - run: yarn install
      - name: build:awsqs-kubernetes-resource
        run: npx projen build:awsqs-kubernetes-resource
      - name: Upload artifact
        uses: actions/upload-artifact@v2.1.1
        if: always()
        with:
          name: packages/@cdk-cloudformation-types/awsqs-kubernetes-resource/dist
          path: packages/@cdk-cloudformation-types/awsqs-kubernetes-resource/dist
    container:
      image: jsii/superchain:1-buster-slim
