# ~~ Generated by projen. To modify, edit .projenrc.js and run "npx projen".

name: release-datadog-monitors-monitor
on:
  workflow_dispatch: {}
jobs:
  build:
    runs-on: ubuntu-latest
    permissions:
      contents: read
    steps:
      - name: Checkout
        uses: actions/checkout@v2
      - name: Set git identity
        run: |-
          git config user.name "github-actions"
          git config user.email "github-actions@github.com"
      - name: build:datadog-monitors-monitor
        run: npx projen build:datadog-monitors-monitor
      - name: Upload artifact
        uses: actions/upload-artifact@v2.1.1
        if: always()
        with:
          name: packages/@cdk-cloudformation-types/datadog-monitors-monitor/dist
          path: packages/@cdk-cloudformation-types/datadog-monitors-monitor/dist
    container:
      image: jsii/superchain:1-buster-slim
