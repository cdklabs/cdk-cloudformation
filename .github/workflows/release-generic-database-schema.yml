# ~~ Generated by projen. To modify, edit .projenrc.js and run "npx projen".

name: release-generic-database-schema
on:
  workflow_dispatch: {}
jobs:
  build:
    runs-on: ubuntu-latest
    permissions:
      contents: read
    steps:
      - name: Checkout
        uses: actions/checkout@v2
      - name: Set git identity
        run: |-
          git config user.name "github-actions"
          git config user.email "github-actions@github.com"
      - name: build:generic-database-schema
        run: npx projen build:generic-database-schema
      - name: Upload artifact
        uses: actions/upload-artifact@v2.1.1
        if: always()
        with:
          name: packages/@cdk-cloudformation-types/generic-database-schema/dist
          path: packages/@cdk-cloudformation-types/generic-database-schema/dist
    container:
      image: jsii/superchain:1-buster-slim
