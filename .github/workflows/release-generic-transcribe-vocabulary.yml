# ~~ Generated by projen. To modify, edit .projenrc.js and run "npx projen".

name: release-generic-transcribe-vocabulary
on:
  push:
    branches:
      - main
  workflow_dispatch: {}
jobs:
  build:
    runs-on: ubuntu-latest
    permissions:
      contents: read
    steps:
      - name: Checkout
        uses: actions/checkout@v2
      - name: Set git identity
        run: |-
          git config user.name "github-actions"
          git config user.email "github-actions@github.com"
      - name: build:generic-transcribe-vocabulary
        run: npx projen build:generic-transcribe-vocabulary
      - name: Upload artifact
        uses: actions/upload-artifact@v2.1.1
        if: always()
        with:
          name: packages/@cdk-cloudformation-types/generic-transcribe-vocabulary/dist
          path: packages/@cdk-cloudformation-types/generic-transcribe-vocabulary/dist
    container:
      image: jsii/superchain:1-buster-slim
