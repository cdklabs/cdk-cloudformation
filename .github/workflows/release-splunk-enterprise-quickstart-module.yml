# ~~ Generated by projen. To modify, edit .projenrc.js and run "npx projen".

name: release-splunk-enterprise-quickstart-module
on:
  push:
    branches:
      - main
  workflow_dispatch: {}
jobs:
  build:
    runs-on: ubuntu-latest
    permissions:
      contents: read
    steps:
      - name: Checkout
        uses: actions/checkout@v2
      - name: Set git identity
        run: |-
          git config user.name "github-actions"
          git config user.email "github-actions@github.com"
      - run: yarn install
      - name: build:splunk-enterprise-quickstart-module
        run: npx projen build:splunk-enterprise-quickstart-module
      - run: mv packages/@cdk-cloudformation/splunk-enterprise-quickstart-module/dist .
      - name: Upload artifact
        uses: actions/upload-artifact@v2.1.1
        if: always()
        with:
          name: dist
          path: dist
    container:
      image: jsii/superchain:1-buster-slim
