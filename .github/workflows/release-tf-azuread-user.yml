# ~~ Generated by projen. To modify, edit .projenrc.js and run "npx projen".

name: release-tf-azuread-user
on:
  push:
    branches:
      - main
  workflow_dispatch: {}
jobs:
  build:
    runs-on: ubuntu-latest
    permissions:
      contents: read
    steps:
      - name: Checkout
        uses: actions/checkout@v2
      - name: Set git identity
        run: |-
          git config user.name "github-actions"
          git config user.email "github-actions@github.com"
      - name: build:tf-azuread-user
        run: npx projen build:tf-azuread-user
      - name: Upload artifact
        uses: actions/upload-artifact@v2.1.1
        if: always()
        with:
          name: packages/@cdk-cloudformation-types/tf-azuread-user/dist
          path: packages/@cdk-cloudformation-types/tf-azuread-user/dist
    container:
      image: jsii/superchain:1-buster-slim
